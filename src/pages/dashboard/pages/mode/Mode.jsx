import React from "react";

function Mode() {
  return (
    <div
      style={{
        width: "100%",
        display: "flex",
        justifyContent: "center",
        flexDirection: "column",
        alignItems: "center",
      }}
      className="mode"
    >
      Mode
    </div>
  );
}

export default Mode;
